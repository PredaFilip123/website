/*const myAdress =
{
    Street: 'Ucea',
    Block: 'P7',
    Apartment: 46,
    Country: 'Romania',
    City: 'Bucharest',
};

console.log(`Adresa mea este aleea ${myAdress.Street} bloc ${myAdress.Block} apartament ${myAdress.Apartment} in ${myAdress.City},${myAdress.Country}`);

const midterms =
{
    Danielle: 96,
    Thomas: 81,
};

midterms.Thomas = 83;

console.log(midterms.Thomas, midterms.Danielle);

const comments = [
    {
        username: 'Tammy',
        text: 'Lmao',
        votes: 9
    },
    {
        username: 'FishBoy',
        text: 'True',
        votes: 156093,
    },
]

console.log(`This is the number of upvotes ${comments[1].votes} ${comments[1].username} has.`);


for (let i = 0; i < 10; i++) {
    console.log(i);
}*/

//DON'T RUN THIS CODE

// for (let i = 100; i >= 0; i -= 10) {
//     console.log(i);
// }

/*let input = prompt("Write something");
while (true) {
    input = prompt(input);
    if (input === 'STOP') {
        break;
    }
}
console.log("Fine");*/

//<-----GUESSING GAME----->

/*let Max = parseInt(prompt("Enter a maximum number"));
let attempts = 0;
const ourNum = Math.floor(Math.random() * Max) + 1;
while (!Max) {
    Max = parseInt(prompt("Please enter a valid number"));
}

let guess = parseInt(prompt("Enter your first guess"));

while (parseInt(guess) !== ourNum) {
    if (guess === 'Q') break;
    attempts++;
    if (guess > ourNum) {
        guess = prompt("Too high. Try again");
    }
    else {
        guess = prompt("Too low. Try again");
    }
}
if (guess === 'Q') {
    console.log('EXITING NOW');
}
else {
    console.log(`Congratulations. It took you ${attempts} tries.`);
}*/









